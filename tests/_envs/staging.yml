# `staging` environment config goes here
modules:
  config:
      WebDriver:
        url: https://3ds-mpi.staging.modirum.com/requestor/requestor
      Db:
        dsn: 'pgsql:host=10.0.129.73;dbname=postgres'
        user: 'hs'
        password: '111111'